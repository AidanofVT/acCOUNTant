NCurses/PDCurses-based feature-rich command line timer.

IF COMPILING ON WINDOWS:
Place the contents of this repository (not a folder containing the contents) into the "wincon" directory of the PDCurses repository. Open with VS Code and "Terminal > Run build task."